# V0.12-B3

B3 evaluates semantic-first reranking over hybrid candidates plus a configurable quality gate. See `b3/B3_RESULTS.json` for measured configurations.

**Status:** experimental / benchmark-only.

**Important:** the semantic encoder is deterministic and synthetic for reproducibility; it is not a production embedding model.
